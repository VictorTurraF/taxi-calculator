import { NumericFormat } from "react-number-format";
import styled from "styled-components";
import { InputStyles } from "../Input/styles";

export const MoneyFormat = styled(NumericFormat)`
  ${InputStyles}
`